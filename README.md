# 跨区域电算协同调度平台

基于 Next.js 的前端可视化原型，包含总览、触发中心、调度任务、资源管理、接入管理和系统管理等功能界面。目前使用模拟数据，便于开展业务流程、数据指标和交互设计验证。

## 本地运行

```powershell
pnpm install
pnpm dev
```

## 发布方式

- 默认构建用于现有 Sites 演示环境。
- 推送至 `main` 分支后，GitHub Actions 自动构建并发布 GitHub Pages。
- 后续接入真实后端时，前端通过 API 网关访问后端服务，数据库及接口凭据不进入前端仓库。

